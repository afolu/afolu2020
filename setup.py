from setuptools import setup, find_packages

setup(name='afolu',
      version='0.0.1',
      url="https://github.com/afolu/afolu2020.git",
      packages=find_packages(),
      python_requires='>=3.6'
      )
